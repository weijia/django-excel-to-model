============
Installation
============

At the command line::

    $ easy_install django-excel-to-model

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-excel-to-model
    $ pip install django-excel-to-model
